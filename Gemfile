source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
# Явно указываем плагины (входит в github-pages, но для локальной разработки можно оставить):
gem "jekyll-feed"
gem "jekyll-seo-tag"

# Тема (github-pages уже включает minima совместимую версию)
gem "minima"

# Закрепить версию Ruby при необходимости (узнать: ruby -v)
# ruby "3.2.2"

